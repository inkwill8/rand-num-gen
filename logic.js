// Find the input on the good ol' DOM
const input = document.querySelector('input');

// Find button on DOM
const pressMe = document.querySelector('button');

// Add event listener to the button
pressMe.addEventListener(onClick);

// Define the function
function 

// Discovered this while reading 'functions' in MDN webdocs.
// So, we're using this method to make JS select a random number between 0 and 1
let random = Math.round(Math.random() * 100);

